{
  "name": "lerna-melt-cover-cloud",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "start:cloud": "lerna run start --scope=melt-cover-cloud --stream",
    "start:shop": "lerna run start --scope=melt-cover-cloud-shop --stream",
    "build-dev:cloud": "lerna run build:dev -d -s --scope=melt-cover-cloud --stream",
    "build-dev:shop": "lerna run build:dev -d -s --scope=melt-cover-cloud-shop --stream",
    "build:cloud": "lerna run build --scope=melt-cover-cloud --stream",
    "build:shop": "lerna run build --scope=melt-cover-cloud-shop --stream"
  },
  "workspaces": [
    "packages/*"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
